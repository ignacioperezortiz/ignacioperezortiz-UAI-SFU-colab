\documentclass[11pt,a4paper]{article}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[english]{babel}
\frenchspacing                     % spanish babel set this implicitly; keeps the original spacing
\usepackage{amsmath}
\usepackage{newtxtext,newtxmath}   % provides \mathbb; amssymb would clash with \Bbbk
\usepackage{xcolor}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{microtype}
\usepackage[most]{tcolorbox}
\usepackage{titlesec}
\usepackage{fancyhdr}
\usepackage[a4paper,top=26mm,bottom=24mm,left=25mm,right=25mm,headsep=9mm]{geometry}
\usepackage{array}

% ---------------------------------------------------------------- UAI / SFU palette
\definecolor{uaired}{HTML}{A6192E}   % UAI institutional red (accent and marks of what is new)
\definecolor{sfuslate}{HTML}{54585A} % SFU grey
\definecolor{ink}{HTML}{1A1A1C}
\definecolor{muted}{HTML}{585C60}
\definecolor{faint}{HTML}{8B8F93}
\definecolor{rule}{HTML}{E4E2E2}
\definecolor{soft}{HTML}{F4E2E5}
\definecolor{paper}{HTML}{F7F6F6}

\color{ink}

% ---------------------------------------------------------------- mark of what is new
\newcommand{\nw}[1]{\textcolor{uaired}{#1}}
\definecolor{propblue}{HTML}{1F4E79}  % petrol blue: proposed, not yet implemented
\newcommand{\prop}[1]{\textcolor{propblue}{#1}}
\newtcolorbox{nuevo}{
  enhanced, breakable, colback=white, colframe=uaired,
  boxrule=0pt, leftrule=1.6pt, arc=0pt, outer arc=0pt,
  left=9pt, right=4pt, top=5pt, bottom=5pt, boxsep=0pt
}
\newtcolorbox{keybox}{
  enhanced, breakable, colback=paper, colframe=rule,
  boxrule=0.5pt, arc=1.5pt, left=10pt, right=10pt, top=7pt, bottom=7pt, boxsep=0pt
}

% ---------------------------------------------------------------- headings
\titleformat{\section}{\normalfont\large\bfseries\color{ink}}{\textcolor{uaired}{\thesection}}{0.7em}{}
\titleformat{\subsection}{\normalfont\normalsize\bfseries\color{sfuslate}}{\textcolor{sfuslate}{\thesubsection}}{0.6em}{}
\titlespacing*{\section}{0pt}{13pt}{6pt}
\titlespacing*{\subsection}{0pt}{10pt}{3pt}

\pagestyle{fancy}\fancyhf{}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\headrule}{\hbox to\headwidth{\color{rule}\leaders\hrule height \headrulewidth\hfill}}
\fancyhead[L]{\footnotesize\color{faint}\textsc{FOR with multiple aggregators and fairness}}
\fancyhead[R]{\footnotesize\color{faint}UAI\,--\,SFU}
\fancyfoot[C]{\footnotesize\color{faint}\thepage}

\setlength{\parindent}{0pt}
\setlength{\parskip}{4pt}
\renewcommand{\arraystretch}{1.12}
\setlength{\abovedisplayskip}{6pt}\setlength{\belowdisplayskip}{6pt}
\setlength{\abovedisplayshortskip}{3pt}\setlength{\belowdisplayshortskip}{3pt}

% ---------------------------------------------------------------- notation
\newcommand{\Nset}{\mathcal{N}}   \newcommand{\Lset}{\mathcal{L}}
\newcommand{\Tset}{\mathcal{K}}   \newcommand{\Dset}{\mathcal{D}}
\newcommand{\Bset}{\mathcal{B}}   \newcommand{\Fset}{\mathcal{F}}
\newcommand{\Hset}{\mathcal{H}}   \newcommand{\Aset}{\mathcal{A}}
\newcommand{\vre}{v^{\mathrm{re}}} \newcommand{\vim}{v^{\mathrm{im}}}
\newcommand{\ire}{i^{\mathrm{re}}} \newcommand{\iim}{i^{\mathrm{im}}}
\newcommand{\tstar}{t^{\star}}
\newcommand{\pch}{p^{\mathrm{ch}}} \newcommand{\pdis}{p^{\mathrm{dis}}}
\newcommand{\ireL}{i^{\mathrm{re,L}}} \newcommand{\ireG}{i^{\mathrm{re,G}}}

\begin{document}

% ================================================================= COVER
\thispagestyle{empty}
\vspace*{4mm}
\noindent
\begin{minipage}[c]{0.46\textwidth}\raggedright
  \includegraphics[height=13mm]{logo-uai.png}
\end{minipage}\hfill
\begin{minipage}[c]{0.46\textwidth}\raggedleft
  \includegraphics[height=13mm]{logo-sfu.png}
\end{minipage}

\vspace{26mm}

{\color{uaired}\rule{34mm}{2.2pt}}

\vspace{7mm}

{\fontsize{15}{18}\selectfont\color{muted}\textsc{Feasible operating region}\par}

\vspace{4mm}

{\hyphenpenalty=10000\exhyphenpenalty=10000\raggedright
\fontsize{26}{31}\selectfont\bfseries
Multiple aggregators\\ and fairness criteria\\ in the rolling-horizon OPF\par}

\vspace{6mm}

{\fontsize{13}{18}\selectfont\color{muted}
Orchestration of the directional sweep, reduction of the computation time\\
and the resulting mathematical formulation\par}

\vspace{20mm}

{\large\bfseries Ignacio Pérez Ortiz\par}
\vspace{1.5mm}
{\color{muted}Master of Science in Engineering\\
Universidad Adolfo Ibáñez \quad\textbf{\textcolor{rule}{|}}\quad Collaboration with Simon Fraser University\par}

\vspace{4mm}
{\color{faint}30 July 2026 \quad\textbf{\textcolor{rule}{|}}\quad revised 5 August 2026\par}

\vfill

\begin{keybox}
\textbf{Reading convention.} Everything that appears \nw{in red} is \nw{new}: it did not exist in the
inherited formulation. The rest is reproduced unchanged and is included so that the model is
explicit from beginning to end. Part~1 does not alter any equation --- it is exclusively
orchestration and performance, and its output is bit-identical to that of the previous model.

\vspace{2pt}
\textbf{\prop{In blue}} is what is \prop{proposed and not yet implemented}: it is included so that it can be discussed, and it is not part of any measured result in this document.
\end{keybox}

\vspace{3mm}
{\footnotesize\color{faint}
Unbalanced three-phase current-injection OPF model, in AIMMS with CPLEX.
Case study: low-voltage feeder, transformer TR9.\par}


\newpage

% ================================================================= PART 1
\section{Rolling-horizon orchestration and computation time}

The rolling horizon runs through 48 half-hour periods. At each ``now'' period the day is rotated so
that \emph{now} is slot~1 and the \textbf{service block} $\tstar$ is slot~2; a committed dispatch is
solved, it is executed, and 12 directions are swept to trace the region offered at $\tstar$. The
formulation did not change: what changed is \emph{what is solved and how many times}.

Both changes were found by profiling the existing sweep, and both are safe only because of how that
sweep was already built: the 12 directions genuinely share one feasible region, and the un-rotated
inputs are already frozen once per run. What changed is the placement of work that was reasonable
when the sweep was written and no longer pays off in the configuration now used in production, a
continuous QCP solved by barrier. Reviewed and agreed before pushing.

\subsection{The loop, before and after}

\begin{minipage}[t]{0.475\textwidth}
{\small\textbf{Before} --- 25 solves and 13 loads per period}
\vspace{2pt}\hrule height 0.4pt
\vspace{4pt}
{\footnotesize\ttfamily
for each period $t$:\\
\hspace*{2mm}load DB + Excel\\
\hspace*{2mm}solve MinImports \hfill(1)\\
\hspace*{2mm}execute slot 1, advance SOC\\
\hspace*{2mm}for each direction $\theta_k$, $k=1..12$:\\
\hspace*{6mm}\textcolor{uaired}{load DB + Excel}\\
\hspace*{6mm}\textcolor{uaired}{solve MinImportsMaxReverseP}\\
\hspace*{6mm}solve FOR\_Vertex($\theta_k$)\\
}
\end{minipage}\hfill
\begin{minipage}[t]{0.475\textwidth}
{\small\textbf{Now} --- 13 solves and 1 load per period}
\vspace{2pt}\hrule height 0.4pt
\vspace{4pt}
{\footnotesize\ttfamily
load DB + Excel \textcolor{uaired}{(once per run)}\\
for each period $t$:\\
\hspace*{2mm}\textcolor{uaired}{light reset}\\
\hspace*{2mm}solve MinImports \hfill(1)\\
\hspace*{2mm}execute slot 1, advance SOC\\
\hspace*{2mm}for each direction $\theta_k$, $k=1..12$:\\
\hspace*{6mm}\textcolor{uaired}{light reset}\\
\hspace*{6mm}solve FOR\_Vertex($\theta_k$)\\
}
\end{minipage}

\vspace{6pt}

From one period to the next \textbf{one single thing} is carried over: the realised state of charge
$e_{b}^{0}$, which advances with the executed dispatch of slot~1 and comes out of the
\emph{baseline}, not out of the sweep. The sweep is handed two: that $e_b^0$ and the dispatch of
slot~1 \emph{fixed}, which guarantees that the offered region is reachable from what the batteries
actually do.

\subsection{Where the time went}

The removed solve, $\min\ \mathrm{OF}^{\text{imp}+\text{rev}}$, ran once per direction.
Its objective \textbf{does not contain the angle}, and within a period the 12 directions share
exactly the same feasible region: same reset, same data rotation, same $e_b^0$ and the same fixed
slot~1. Therefore

\begin{keybox}
The 12 solves of a period were \textbf{the same optimization problem} --- same matrix, same
objective, same solution --- solved 12 times. Its purpose was to seed the vertex solve: the answer
was kept only as the starting point (\emph{warm start}), and nothing else downstream read it.
\end{keybox}

\subsection{The interventions}

\begin{description}[leftmargin=1.6em,itemsep=3pt,topsep=3pt]
  \item[\nw{(E)} Splitting the loader.] The reading of the database and Excel was split into a
  \emph{static} half, which runs once per run, and a per-solve \emph{reset} that restores only the
  variables that the emptying destroys: the fixing of $q_{b,t}$, the flat voltage seed and the
  linearization point. Data loads per period: $13 \rightarrow 1$.
  \item[\nw{(A)} Removal of the per-direction warm-start solve.] It was suppressed. Solves per
  period: $25 \rightarrow 13$. It remains behind a switch that restores the previous behaviour, and
  it is kept \emph{active} in the MIQCP validations, where the start channel is real.
  \item[\nw{(C)} Fix in the diagnostic passes.] On splitting the loader, the retries of failed
  directions stopped inheriting the experimental SOC floor; it is re-applied explicitly so that the
  retry runs under the same condition as the direction that failed.
\end{description}

\subsection{Interior point: why the warm start did not help here}

\textbf{What a \emph{warm start} is.} Handing the solver a starting point already close to the
answer, so that less ground is left for it to cover. It is standard practice and it usually helps.

\textbf{Why it did not help here.} It depends on the method, and the two available ones look for the
optimum in opposite ways:

\begin{center}\small
\begin{tabular}{@{}p{0.20\textwidth} p{0.36\textwidth} p{0.36\textwidth}@{}}
\toprule
& \textbf{Simplex} & \textbf{Barrier (interior point)}\\
\midrule
Where it advances & along the \textbf{edge}, jumping from vertex to vertex & through the
\textbf{interior}, following the central path\\
\addlinespace[2pt]
Ideal starting point & a vertex close to the optimum --- a basis & a \textbf{well-centred} point,
far from every active constraint\\
\addlinespace[2pt]
Effect of starting on the boundary & it brings it closer: few jumps are missing & it
\textbf{penalises} it: it must first re-centre\\
\bottomrule
\end{tabular}
\end{center}

The point that the code handed over was the optimum of \emph{another} problem, the minimum-cost one.
And the optimum of a convex program with a linear objective is, by construction, an \textbf{extreme
point}: stuck against the boundary, with several active constraints. It is exactly what an
interior-point method does not want --- hence the name of the method: there is a barrier term that
pushes it \emph{away} from the constraints, and starting on one of them forces that work to be
undone before it can advance. In the corridor image: barrier walks down the middle, and it was being
handed the point flattened against the wall.

\textbf{The change.} Suppress that solve. \emph{No equation was touched.} The vertex solve now
starts from the flat point that the reset leaves --- all voltages at their balanced nominal value
---, comfortably interior and with no active constraints, which is just what barrier needs.

\textbf{Measured result:} $1.27$ s $\rightarrow$ $0.62$ s per vertex solve. \emph{Half, from no
longer helping it.}

\begin{keybox}
\textbf{An honest nuance about the mechanism.} What is verified is the \emph{measurement}:
2.05$\times$ per solve, with bit-identical output. The explanation of the badly centred point is the
best supported one, but it has a known tension: on the continuous path the solver does not receive a
basis --- that channel exists only on the integer path --- and the first solve, also a barrier one,
does not produce one to hand over either. That the difference is measured all the same indicates
that something of the previous state has an influence through a route that was not identified. The
\emph{what} is proven; the \emph{why} is a supported hypothesis, not a verified fact.
\end{keybox}

Note that, even accepting the warm-start theory, \textbf{11 of the 12 solves were redundant
anyway}: it was enough to solve it once per period and reuse the point. Both programs declare the
same sets of constraints and variables, so that they generate a row-for-row identical matrix and
differ only in the objective column --- which is why removing one does not alter the other.

Numerical options kept: feasibility tolerance $10^{-3}$, barrier convergence tolerance
$10^{-2}$, aggressive scaling, and the compact generator of quadratic programs --- needed because
the inherited generator expanded the quadratics and exhausted the memory towards period
37 of 48.

\subsection{Measured results}

\begin{center}\small
\begin{tabular}{l r r r}
\toprule
& \textbf{Before} & \textbf{Now} & \textbf{Factor} \\
\midrule
\multicolumn{4}{l}{\itshape Micro network --- TR9\_two, 12 batteries, 48 periods $\times$ 12 directions}\\
Total time & 41\,m\,22\,s & 10\,m\,12\,s & $4.06\times$ \\
Vertex solves & 731.0 s & 358.7 s & $2.05\times$ \\
Per solve & 1.27 s & 0.62 s & \\
\addlinespace
\multicolumn{4}{l}{\itshape Production --- TR9, 90 prosumers}\\
Total time & $\sim$28 h & \textbf{3 h 40 m} & $\mathbf{\sim7.6\times}$ \\
Vertex solves & 30\,213.4 s & 8\,440.7 s & $3.58\times$ \\
Per solve & 52.45 s & 14.65 s & \\
\bottomrule
\end{tabular}
\end{center}

\begin{keybox}
\textbf{Equivalence verified.} $\max|\Delta\,\mathrm{proj}| = 0.000\mathrm{e}{+}00$ in all four
runs; 576/576 optimal vertices; byte-identical state-of-charge file; the six metrics of the
deliverable reproduced exactly. The acceptance criterion is the \textbf{projection}
$\theta^\top(P,Q)$, not the equality of $(P,Q)$: a flat face perpendicular to the sweep direction
turns the argument of the maximum into a segment, and the returned point may legitimately slide along it.
\end{keybox}

\vspace{2pt}


\vspace{2pt}
The real bottleneck was not the data reading but the \textbf{generation of the mathematical
program}: on TR9 each generation of a QCP with 90 batteries and 341 buses costs of the order of
30\,s, and \nw{(A)} removes 12 generations per period on top of 12 solves.

\subsection{\nw{Second phase: one matrix generation per period}}

\begin{nuevo}
\textbf{\nw{New.}} The first phase exposed the dominant remaining cost: \textbf{generating} the
mathematical program. A \texttt{solve} statement in AIMMS does two things --- it walks the algebraic
model and expands it into explicit matrix rows, and only then calls the solver --- and that
generation happens on \emph{every} \texttt{solve}, whether or not the data changed, because no
matrix is cached between statements. On the whole feeder that expansion is 7.5 million rows and
costs $\sim$665\,s, against $\sim$200\,s of actual solving.

The \textbf{GMP} library (\emph{Generated Mathematical Program}) makes it possible to generate that
matrix once and keep it in memory to work on: it can be modified and re-solved without being rebuilt.
Since the 12 directions of a period \textbf{share one feasible region} and differ only in the
objective, one generation can serve all 12 solves: per direction only the two coefficients of the row
defining $\mathrm{OF}_{\mathrm{FOR}}$ are rewritten.
\end{nuevo}

\textbf{Three interventions.} \nw{(G)} the sweep is served by a matrix generated once per period;
\nw{(B)} the committed dispatch is solved on \emph{that same} instance by switching the objective
column, which removes the second generation of the period; \nw{(F)} the results file is closed after
every vertex, so an interruption keeps what has already been solved.

Two details make \nw{(B)} exact. The slot-1 dispatch moves from the \texttt{.nonvar} property ---
which belongs to the \emph{model} and is read when the matrix is built, so setting it afterwards has
no effect --- to fixing the bounds of the \emph{instance}. And the fairness rows, which the sweep must
see and the committed dispatch must not, are \textbf{deactivated} for the dispatch solve and
reactivated for the sweep: a deactivated row stays in the instance but is not passed to the solver,
which is exactly what regenerating without it used to achieve. That is what lets one matrix per
period serve \emph{any} fairness level, not only the case with no criterion.

\subsection{\nw{Results of the second phase}}

\textbf{Production TR9.} The three measured criteria, 48 periods $\times$ 12 directions each, with the
state of charge carried forward between periods:

\begin{center}\small
\begin{tabular}{l r r r}
\toprule
Criterion & $\max|\Delta\,\mathrm{proj}|$ & Status & Result \\
\midrule
$m=0$ (no fairness)   & $1.0\mathrm{e}{-}06$ & 576/576 optimal & \nw{equivalent} \\
$m=1$ (prosumers)     & $1.0\mathrm{e}{-}06$ & 576/576 optimal & \nw{equivalent} \\
$m=2$ (aggregators)   & $\mathbf{0.000\mathrm{e}{+}00}$ & 576/576 optimal & \nw{equivalent} \\
\bottomrule
\end{tabular}
\end{center}

The tolerance is $2\mathrm{e}{-}06$ and the file prints six decimals, so $1\mathrm{e}{-}06$ p.u. is
\emph{one} unit of the last printed digit: 1\,W at $S_b=1$\,MW. In time, the three runs together go
from \textbf{12\,h\,17\,m} to \textbf{9\,h\,37\,m}, a \textbf{$-22$\,\%}. Accumulated with the first
phase, a production run of TR9 goes from $\sim$28\,h to $\sim$3\,h\,10\,m.

\vspace{2mm}
\textbf{\nw{Whole feeder.}} This is the underlying question --- whether the model can be run over the
entire network --- and it is where generation dominates, because it scales with problem size while
the solver does not scale in the same proportion:

\begin{center}\small
\begin{tabular}{l r r r}
\toprule
& \textbf{Wall/vertex} & \textbf{Solver} & \textbf{Reconstruction} \\
\midrule
Without \nw{(G)} & 844--904 s & 183--238 s & $\sim$665 s \\
With \nw{(G)}    & \textbf{$\sim$240 s} & 236 s & \nw{$\sim$0} \\
\bottomrule
\end{tabular}
\end{center}

Wall-clock converges to solver time: the per-direction reconstruction disappears, a factor
$\sim3.6\times$. That in turn makes the cost of the \textbf{exact} formulation on the whole feeder
measurable, which is the one production uses:

\begin{center}\small
\begin{tabular}{l r r r r}
\toprule
& & & \multicolumn{2}{c}{\textbf{Simulated day (48 periods)}}\\
\cmidrule(l){4-5}
Formulation & Solver/vertex & Generation & \nw{(G)} only & \nw{(G)}+\nw{(B)} \\
\midrule
Linearized & $\sim$236 s & $\sim$665 s & $\sim$2.4 days & $\sim$2.1 days \\
\nw{Exact} & \nw{287.24 s} & \nw{$\sim$1\,005 s} & \nw{$\sim$3.5 days} & \nw{$\sim$2.9 days} \\
\bottomrule
\end{tabular}
\end{center}

\vspace{-1mm}
The whole-feeder measurement was taken with \nw{(G)} alone, which is what removes the 11 redundant
reconstructions of the sweep. \nw{(B)} additionally removes the generation of the committed dispatch
--- the only one left in the period --- and is therefore worth a further \textbf{$-15$\,\% to
$-16$\,\%} depending on the formulation. The last two columns are projections over 48 periods.

\begin{keybox}
\textbf{\nw{The exact formulation solves on the whole feeder.}} 12 of 12 directions optimal, no solve
near the time limit, with true $V_{\max}=1.10000$ p.u. and overvoltage violation at numerical zero.
The matrix is 7\,534\,679 rows, 6\,095\,687 columns and 26\,929\,278 nonzeros. It costs $+22$\,\% per
vertex over the linearized one and $+43$\,\% per period once generation is counted: \textbf{a
fraction, not a multiple}.

This matters because the linearized variant relaxes the voltage limit --- its tangent plane admits
points the exact constraint rejects --- and on the same period it \textbf{overstates the region by
between 0.9\,\% and 1.4\,\%}, in the optimistic direction. The simulated-day figures are projections
from representative periods, not complete runs.
\end{keybox}

\vspace{2pt}
As a practical consequence, the model's procedures can be launched \textbf{from the terminal} without
opening the interface (\texttt{AimmsCmd.exe -{}-run-only <Procedure> <project>.aimms}), which makes
these long runs scriptable and observable from outside.


\newpage

% ================================================================= PART 2
\section{Formulation: multiple aggregators and fairness criteria}

Unbalanced three-phase current-injection model, with linearization of the load and generation
injections around a point $(\vre_0,\vim_0)$ and exact quadratic constraints for maximum voltage and
inverter capacity. Everything in \nw{red} is new.

\subsection{Sets and indices}

\begin{center}\small
\begin{tabular}{@{}l l@{\hspace{2.2em}} l l@{}}
$\Nset$ & buses $n$ & $\Bset$ & batteries / prosumers $b$\\
$\Lset$ & lines $\ell$ & $\Fset=\{1,2,3\}$ & phases $f,h$\\
$\Tset$ & transformers $k$ & $\Hset=\{1,\dots,48\}$ & periods $t$\\
$\Dset$ & loads with PV $d$ & \nw{$\Aset$} & \nw{aggregators $\alpha$}\\
\end{tabular}
\end{center}

$\tstar$ denotes the service block; in the rolling horizon $\tstar = 2$ after the rotation.
\nw{$a(b)\in\Aset$} is \nw{the aggregator of prosumer $b$}, an input datum.

\subsection{Objective function of the directional sweep}

For each direction $\theta_k = 30^\circ(k-1)$, $k=1,\dots,12$:

\begin{equation}
\min\ \ -\bigl(\cos\theta_k\, P_{\tstar} + \sin\theta_k\, Q_{\tstar}\bigr)
\end{equation}

with the power at the connection point, positive from grid towards feeder,

\begin{align}
P_t &= \sum_{g}\ \vre_{\mathrm{pcc},f(g)}\, \ire_{g,t} + \vim_{\mathrm{pcc},f(g)}\, \iim_{g,t}\\
Q_t &= \sum_{g}\ \vim_{\mathrm{pcc},f(g)}\, \ire_{g,t} - \vre_{\mathrm{pcc},f(g)}\, \iim_{g,t}
\end{align}

Minimising $-\theta_k^\top(P,Q)$ evaluates the \emph{support function} of the feasible set in the
direction $\theta_k$; by rotating $\theta$ the boundary of the region is traced.

\subsection{Network}

The voltage is anchored at the reference bus $r$, fixed and balanced, and it is what sets the origin
of the whole system:
\begin{equation}
\vre_{r,f,t} = \begin{cases}1 & f=1\\ -\tfrac12 & f\in\{2,3\}\end{cases}
\qquad
\vim_{r,f,t} = \begin{cases}0 & f=1\\ -\tfrac{\sqrt3}{2} & f=2\\ +\tfrac{\sqrt3}{2} & f=3\end{cases}
\end{equation}

Current balance at each bus and phase:
\begin{equation}
\sum_{g\in n,f}\!\ire_{g,t} + \!\!\sum_{\ell:\,\mathrm{to}(\ell)=n}\!\!\ire_{\ell,f,t}
+ \!\!\sum_{k:\,\mathrm{to}(k)=n}\!\!\ire_{k,f,n,t}
- \!\!\sum_{\ell:\,\mathrm{from}(\ell)=n}\!\!\ire_{\ell,f,t}
- \!\!\sum_{k:\,\mathrm{from}(k)=n}\!\!\ire_{k,f,n,t}
= \!\!\sum_{d\in n,f}\!\!\bigl(\ireL_{d,t}-\ireG_{d,t}\bigr) + \!\!\sum_{b\in n,f}\!\!\ire_{b,t}
\end{equation}
and its analogue in the imaginary part. Voltage drop on lines, with mutual coupling:
\begin{align}
\vre_{\mathrm{from}(\ell),f,t}-\vre_{\mathrm{to}(\ell),f,t}
&= \sum_{h\neq f}\!\bigl(R^{m}_{\ell}\ire_{\ell,h,t}-X^{m}_{\ell}\iim_{\ell,h,t}\bigr)
 + R^{s}_{\ell}\ire_{\ell,f,t}-X^{s}_{\ell}\iim_{\ell,f,t}\\
\vim_{\mathrm{from}(\ell),f,t}-\vim_{\mathrm{to}(\ell),f,t}
&= \sum_{h\neq f}\!\bigl(X^{m}_{\ell}\ire_{\ell,h,t}+R^{m}_{\ell}\iim_{\ell,h,t}\bigr)
 + X^{s}_{\ell}\ire_{\ell,f,t}+R^{s}_{\ell}\iim_{\ell,f,t}
\end{align}

Dy transformers with tap ratio $\tau_{k,t}$, coupling phase $f$ with the next two:
\begin{equation}
\vre_{\mathrm{from}(k),f,t} = -\tfrac{\nu_k \tau_{k,t}}{3}\Bigl(2\vre_{\mathrm{to}(k),f\oplus1,t}+\vre_{\mathrm{to}(k),f\oplus2,t}
+ R_k\bigl(2\ire_{k,f\oplus1,\cdot,t}+\ire_{k,f\oplus2,\cdot,t}\bigr) - X_k\bigl(2\iim_{k,f\oplus1,\cdot,t}+\iim_{k,f\oplus2,\cdot,t}\bigr)\Bigr)
\end{equation}
The $\vim$ equation is the same with $R_k$ acting on the imaginary currents and $+X_k$ on the real
ones. The primary--secondary current relation is
$\ire_{k,f,\mathrm{from},t}\,\nu_k\tau_{k,t} = \ire_{k,f,\mathrm{to},t}-\ire_{k,f\oplus1,\mathrm{to},t}$,
and analogous in $\iim$. In transformers without on-load tap changing the tap is fixed,
$\tau_{k,t}=\tau_k^{\mathrm{fix}}$, so that the product $\tau_{k,t}\,v$ introduces no
nonlinearity: the model remains affine in the injections.

Voltage limits --- upper exact, lower on the first-order approximation
$\widehat{V^2}$ --- and thermal limits:
\begin{align}
(\vre_{n,f,t})^2+(\vim_{n,f,t})^2 &\le \overline{V}_n^{\,2},
&\widehat{V^2}_{n,f,t} &\ge \underline{V}_n^{\,2}\\
(\ire_{\ell,f,t})^2+(\iim_{\ell,f,t})^2 &\le \overline{I}_\ell^{\,2},
&(\ire_{k,f,\mathrm{from},t})^2+(\iim_{k,f,\mathrm{from},t})^2 &\le \overline{I}_k^{\,2}
\end{align}
where $\widehat{V^2}_{n,f,t} = (\vre_0)^2+(\vim_0)^2+2\vre_0(\vre-\vre_0)+2\vim_0(\vim-\vim_0)$.
The load and PV injections are linearized in the same way, around $(\vre_0,\vim_0)$:
\begin{equation}
\ireL_{d,t} = \frac{P^0_{d,t}\vre_0 + Q^0_{d,t}\vim_0}{(\vre_0)^2+(\vim_0)^2}
+ \frac{\partial \ireL}{\partial \vre}\bigl(\vre-\vre_0\bigr)
+ \frac{\partial \ireL}{\partial \vim}\bigl(\vim-\vim_0\bigr)
\end{equation}

\subsection{Batteries}

With $p_{b,t}$ the net active power of prosumer $b$ ($p_{b,t}>0$ charging, $p_{b,t}<0$ discharging)
and $q_{b,t}$ the reactive power of the inverter:
\begin{align}
p_{b,t} &= \pch_{b,t}-\pdis_{b,t}, &\pch_{b,t},\ \pdis_{b,t} &\in[0,\overline{P}_b]\\
p_{b,t} &= \vre_{0,b,t}\,\ire_{b,t}+\vim_{0,b,t}\,\iim_{b,t},
&q_{b,t} &= -\vre_{0,b,t}\,\iim_{b,t}+\vim_{0,b,t}\,\ire_{b,t}\\
e_{b,t} &= e^0_b + \frac{\Delta t}{E_b}\sum_{\tau\le t}\Bigl(\eta^{c}_b \pch_{b,\tau}-\frac{\pdis_{b,\tau}}{\eta^{d}_b}\Bigr),
&\underline{\sigma}_b &\le e_{b,t}\le \overline{\sigma}_b
\end{align}
where $e_{b,t}$ is the state of charge as a fraction and $\underline{\sigma}_b,\overline{\sigma}_b$
its bounds --- distinct from the capacity $E_b$, which appears in the denominator and is in units of energy.
Inverter capacity at the service block (exact quadratic cone) and cyclic return of the
state of charge over the window:
\begin{equation}
p_{b,\tstar}^{2}+q_{b,\tstar}^{2}\ \le\ \overline{P}_b^{\,2},
\qquad
\Bigl|\sum_{t\in\Hset}\Bigl(\eta^{c}_b \pch_{b,t}-\frac{\pdis_{b,t}}{\eta^{d}_b}\Bigr)\Bigr| \le \beta E_b/\Delta t
\end{equation}
Reactive power is released only at $\tstar$: $q_{b,t}=0$ for $t\neq\tstar$. In the sweep, the
dispatch of slot~1 is fixed to the committed value: $\pch_{b,1}=\hat{p}^{c}_b$,
$\pdis_{b,1}=\hat{p}^{d}_b$. The model also keeps the inherited constraint
$p_{b,\tstar}\le \kappa\,\overline{P}_b$, where $\kappa=0$ reproduces the assumption of the paper
--- batteries only discharging at the service block --- and $\kappa=1$ is the production
configuration, bidirectional; with $\kappa=1$ the constraint is dominated by
$\pch_{b,\tstar}\le\overline{P}_b$ and is not binding.

\subsection{\nw{Fairness: proportional criterion C1}}

\begin{nuevo}
\textbf{\nw{New.}} Let \nw{$s_b$} be the nominal capacity of prosumer $b$ --- \nw{$s_b=\overline{P}_b$}
is adopted, which in this model is at once the power of the battery and the radius of the
circle of inverter capacity. Let \nw{$\zeta\in\mathbb{R}$} be a \textbf{single free scalar per
solve}, interpretable as the \emph{utilisation factor} of the fleet. With
\nw{$m\in\{0,1,2\}$} the fairness level and \nw{$\chi\in\{0,1\}$} the activation gate:

\vspace{-3mm}
\begin{align}
\nw{p_{b,\tstar}} &\ \nw{=\ \zeta\, s_b} &&\nw{\forall b\in\Bset,}
&&\nw{\text{if } m=1 \ \text{and}\ \chi=1} \label{eq:c1p}\\[2pt]
\nw{\sum_{b:\,a(b)=\alpha} p_{b,\tstar}} &\ \nw{=\ \zeta \!\!\sum_{b:\,a(b)=\alpha}\!\! s_b}
&&\nw{\forall \alpha\in\Aset,} &&\nw{\text{if } m=2 \ \text{and}\ \chi=1} \label{eq:c1a}
\end{align}
\end{nuevo}

\textbf{A single degree of freedom.} The criterion is written with \emph{one} scalar $\zeta$, which
removes \emph{one} degree of freedom. Writing it as $n-1$ pairwise equalities would remove $n-1$ and
would overestimate the cost of fairness by a factor close to two.

\textbf{Scope of each level.} Level~2 constrains only the sums per aggregator; the internal split
of each aggregator remains free. The levels are alternative, not cumulative.

\textbf{Consequence of the sign.} Since $p_{b,\tstar}=\pch-\pdis$, a single $\zeta$ forces the whole
fleet to the same sign: \emph{it forbids one battery to charge while another discharges}. It is the
strictest reading of the criterion, and that is why C1 gives the \textbf{upper bound} on the cost of fairness.

\textbf{Feasibility.} $\zeta=0$ is always admissible, so that C1 \emph{contracts} the region and
never makes it infeasible.

\begin{nuevo}
\textbf{\nw{Corrected (31~Jul).} \nw{The bound on the scalar, and why it is not a constraint.}}
\nw{The bound is stated as a \emph{linear box}, never in modulus: writing $|\zeta|\le1$ would
introduce a nonlinearity for no reason. The correct form is}

\vspace{-3mm}
\begin{equation}
\nw{-1\ \le\ \zeta\ \le\ 1 .}
\label{eq:zbox}
\end{equation}

\nw{That said, \eqref{eq:zbox} \textbf{is not added to the model}, because it holds by construction.
Every battery already satisfies $|p_{b,\tstar}|\le\overline{P}_b$ through its own charge and
discharge limits, which are box bounds on $\pch$ and $\pdis$ and hold at all times, independently of
the fidelity switches. With $s_b=\overline{P}_b$, dividing \eqref{eq:c1p} by the capacity carries
that bound straight onto $\zeta$; at level~2 the same follows from the triangle inequality on the
aggregator sum:}

\vspace{-3mm}
\[
\nw{\Big|\sum_{b:\,a(b)=\alpha} p_{b,\tstar}\Big|\ \le\
\sum_{b:\,a(b)=\alpha} \big|p_{b,\tstar}\big|\ \le\ \sum_{b:\,a(b)=\alpha} s_b .}
\]

\nw{Imposing it would therefore add a redundant row. The \emph{a posteriori} verification shows
further that the bound is \textbf{reached} ---$\max|\zeta|=1.0000$--- and never exceeded: it is an
effective facet of the feasible region, not slack.}

\smallskip
\nw{\textbf{Warning for $m=3$ and $m=4$.} The argument requires the normalising capacity to be \emph{the same quantity} that bounds the left-hand side. When the criterion moves to the prosumer's export the normalisation is no longer $\overline{P}_b$ and the bound no longer comes for free: it is re-derived in \S\ref{sec:exchange}, where it survives on one side only.}
\end{nuevo}

\subsection{\nw{The gate $\chi$}}

$\chi$ takes the value $1$ in the 12 directional solves and $0$ in the solve of the committed
dispatch. The reason is not one of implementation:

\begin{keybox}
$\tstar$ is slot~2 \emph{also} in the baseline, so that without the gate
\eqref{eq:c1p}--\eqref{eq:c1a} would constrain the dispatch executed in slot~1, the trajectory of
the state of charge would diverge from the case without fairness, and the comparison would measure
two mixed effects: the contraction of the region, and a different battery state. Moreover the
objective of the baseline contains no service term at $\tstar$ --- by construction $w_2(\tstar)=0$
--- so imposing a market rule there would constrain an agent that obtains no value at all from
participating in it.
\end{keybox}

\subsection{\nw{Fairness on the exchange: $m=3$ and $m=4$}}\label{sec:exchange}

\begin{nuevo}
\textbf{\nw{New.}} Levels 1 and 2 share out the \emph{battery balance}. The review of 31-Jul-2026
proposed applying the same proportional criterion to the \textbf{prosumer's exchange with the
network}, which is the quantity a flexibility market actually observes: consumption minus PV
generation, plus charge and minus discharge.

\nw{Let $x_b$ be the \textbf{net exchange of prosumer $b$ with the network} at the service block,
positive towards \emph{export}. With $g_{b,t}$ and $c_{b,t}$ the prosumer's PV generation and
consumption --- both input data --- and $p_{b,t}$ the battery balance already defined in \S2.4:}

\vspace{-4mm}
\begin{equation}
\nw{x_{b}\ =\ \underbrace{g_{b,\tstar}-c_{b,\tstar}}_{\text{passive surplus}}
\ -\ \underbrace{p_{b,\tstar}}_{=\ \pch_{b,\tstar}-\pdis_{b,\tstar}}}
\label{eq:xb}
\end{equation}

\nw{Charging the battery ($p_{b,\tstar}>0$) reduces what the prosumer delivers to the network and
discharging increases it. The criterion is then written on $x_b$ instead of on $p_{b,\tstar}$:}

\vspace{-3mm}
\begin{align}
\nw{x_{b}} &\ \nw{=\ \zeta\, s_b} &&\nw{\forall b\in\Bset,}
&&\nw{\text{if } m=3 \ \text{and}\ \chi=1} \label{eq:c1pe}\\[2pt]
\nw{\sum_{b:\,a(b)=\alpha} x_{b}} &\ \nw{=\ \zeta \!\!\sum_{b:\,a(b)=\alpha}\!\! s_b}
&&\nw{\forall \alpha\in\Aset,} &&\nw{\text{if } m=4 \ \text{and}\ \chi=1} \label{eq:c1ae}
\end{align}

\nw{With $m\ge3$ the reference capacity is no longer just the battery nameplate but the export
capability available at the service instant, $s_b=\overline{P}_b+g_{b,\tstar}$, which is the quantity
homogeneous with $x_b$.}
\end{nuevo}

\textbf{\nw{A consequence that is not a defect: the criterion can be infeasible.}} Under $m=1$ the
value $\zeta=0$ parks the fleet and is always admissible, so the criterion only contracts the region.
Under $m=3$ that no longer holds: the passive net load enters as a \textbf{different constant for
each prosumer}, so $\zeta=0$ requires the battery to offset its own net consumption exactly, which it
cannot always do.

\begin{keybox}
\textbf{\nw{Measured evidence}} (micro bed, 3-Aug-2026). In the 22:00 block all twelve directions come
out infeasible: one battery at its state-of-charge floor, with no discharge headroom and 0.93\,kW of
its own consumption, imposes $\zeta\le-0.1315$ on the \emph{whole} fleet, and no single $\zeta$
satisfies the rest simultaneously. \textbf{It is a result about the criterion, not a numerical
failure.}

\nw{The derived bound also changes shape.} Under $m=1$ and $m=2$, $|\zeta|\le1$ holds by construction.
Under $m=3$ and $m=4$ only one side survives: export is bounded by $g_{b,\tstar}+\overline{P}_b\le
s_b$, hence $\zeta\le1$; but import is load plus battery power, and a prosumer whose consumption
exceeds its generation plus its battery can push $\zeta$ below $-1$. It is worth reporting the
realised range rather than assuming it.
\end{keybox}

\subsection{\prop{Proposal: fairness on the available range ($m=5$ and $m=6$)}}

\begin{nuevo}
\textbf{\prop{Proposed, not yet implemented.}} \prop{Criteria $m=1$ to $m=4$ share out in proportion
to what is \emph{installed}. The problem is that the nameplate does not know what the prosumer can do
at this instant: a full battery has full nameplate and zero import capability, and the equality asks
it for something impossible --- which is the origin of the infeasibility of $m=3$.}

\prop{The proposal shares out in proportion to the \textbf{exchange range each prosumer can actually
traverse} at the service block. Since the slot-1 dispatch is already committed when the sweep runs,
both ends of that range are \emph{parameters}:}

\vspace{-2mm}
\begin{align}
\prop{x_b} &\ \prop{\in\ [\,\underline{x}_b,\ \overline{x}_b\,]}, &&
\prop{\underline{x}_b = -\bigl(n_b + \kappa_b\bigr)}, &&
\prop{\overline{x}_b = \delta_b - n_b} \nonumber
\end{align}
\vspace{-6mm}
\begin{align}
\prop{x_b} &\ \prop{=\ \underline{x}_b + \zeta\,\bigl(\overline{x}_b - \underline{x}_b\bigr)}
&&\prop{\forall b\in\Bset,} &&\prop{\text{if } m=5 \ \text{and}\ \chi=1} \label{eq:c1range}\\[4pt]
\prop{\sum_{b:\,a(b)=\alpha} x_b} &\ \prop{=\ \sum_{b:\,a(b)=\alpha}\underline{x}_b
\ +\ \zeta \!\!\sum_{b:\,a(b)=\alpha}\!\! \bigl(\overline{x}_b-\underline{x}_b\bigr)}
&&\prop{\forall \alpha\in\Aset,} &&\prop{\text{if } m=6 \ \text{and}\ \chi=1} \label{eq:c1rangeagg}
\end{align}

\prop{with $n_b = c_{b,\tstar}-g_{b,\tstar}$ the net consumption, and $\kappa_b$ and $\delta_b$ the
charge and discharge headroom the state of charge allows in that block --- all three are
\emph{parameters}, because the slot-1 dispatch is already committed when the sweep runs.}

\prop{Now $\zeta$ is not the fraction of the nameplate but the \textbf{relative position within each
prosumer's own feasible range}, common to the whole fleet. Under $m=6$ the aggregator must reach that
same position \emph{in aggregate}, while the split among its own prosumers stays free --- the same
relation $m=2$ has with $m=1$.}
\end{nuevo}

\begin{keybox}
\prop{\textbf{Why it closes the gap.}} \prop{For any $\zeta\in[0,1]$ the resulting $x_b$ falls inside
the interval by construction, so it is \textbf{never infeasible} and no block is lost. It remains an
\textbf{equality for everyone}, with no one-sided rows and no exceptions that apply only when
convenient. It is \textbf{linear}: the ends are parameters and $\zeta$ multiplies a constant, with no
binaries. The bound $0\le\zeta\le1$ is again \textbf{derived} rather than imposed. And the exclusion
of whoever cannot participate \textbf{emerges from the formulation}: if a prosumer cannot move,
$\overline{x}_b=\underline{x}_b$ and its row reads $x_b=\underline{x}_b$ whatever $\zeta$ is, so it
neither constrains nor is constrained.}

\prop{\textbf{It is a different definition of fairness, not a correction.}} \prop{$m=1$ to $m=4$ share
out in proportion to what is installed; $m=5$ and $m=6$, to what is available. That is why they are
proposed as \emph{new} criteria and do not replace the earlier ones: with the three families
implemented the comparison can be shown measured rather than argued.}
\end{keybox}

\vspace{2pt}
\prop{The alternative route --- patching $m=3$ --- was explored and set aside. Excluding whoever
cannot participate resolves the hard infeasibility but does not cover whoever can do little; relaxing
the row to a one-sided inequality binds from below but does not prevent exporting beyond one's share,
so the criterion would end up applying only when convenient; and making it depend on the sign of
$\zeta$ --- which is an outcome of the solve --- would require a disjunction, that is binaries and a
mixed-integer problem. The answer is a different normalisation, not a better patch.}

\subsection{Cases and nesting}

\begin{center}\small
\begin{tabular}{c l l}
\toprule
$m$ & Case & Constraint active at $\tstar$\\
\midrule
$0$ & No fairness (reference) & --- no row generated\\
$1$ & \nw{Proportional between prosumers} & \nw{\eqref{eq:c1p}}\\
$2$ & \nw{Proportional between aggregators} & \nw{\eqref{eq:c1a}}\\
$3$ & \nw{On the exchange, between prosumers} & \nw{\eqref{eq:c1pe}}\\
$4$ & \nw{On the exchange, between aggregators} & \nw{\eqref{eq:c1ae}}\\
$5$ & \prop{On the available range, between prosumers} & \prop{\eqref{eq:c1range}}\\
$6$ & \prop{On the available range, between aggregators} & \prop{\eqref{eq:c1rangeagg}}\\
\bottomrule
\end{tabular}
\end{center}

With $m=0$ both domains are empty: no row is generated and $\zeta$ remains as a free column with no
coefficients, which presolve removes. That is the reason why the reference case reproduces the
previous model exactly.

\textbf{Nesting.} Level~1 implies level~2: by summing \eqref{eq:c1p} over the prosumers of
an aggregator, \eqref{eq:c1a} is obtained with the same $\zeta$. Therefore
\begin{equation}
\mathcal{X}(m{=}1)\ \subseteq\ \mathcal{X}(m{=}2)\ \subseteq\ \mathcal{X}(m{=}0)
\quad\Longrightarrow\quad
\mathrm{proj}_{\theta}^{(1)}\ \le\ \mathrm{proj}_{\theta}^{(2)}\ \le\ \mathrm{proj}_{\theta}^{(0)}
\ \ \forall\theta,\ \forall t
\end{equation}
which is the acceptance test: a constraint cannot enlarge the feasible set, and any
violation is a defect and not a result.

\nw{The nesting holds within each family.} Levels 1--2 share out the battery balance and levels 3--4
the prosumer's exchange: they are different quantities, so $m=3$ is not a special case of $m=1$ nor
the other way round, and their regions are not comparable by inclusion. Within the exchange family,
summing \eqref{eq:c1pe} over the prosumers of an aggregator does return \eqref{eq:c1ae} with the
same $\zeta$, so $\mathcal{X}(m{=}3)\subseteq\mathcal{X}(m{=}4)$ as before.

\subsection{Results on the micro network ($m=1$, $m=2$)}

Full verification on the 576 vertices: the reference case reproduces the previous model with
exact $\Delta=0$ and byte-identical state of charge; the nesting holds without exception; and the
constraint is satisfied with a maximum residual of $10^{-4}$, an order stricter than the solver's own
feasibility tolerance.

\begin{center}\small
\begin{tabular}{l r r}
\toprule
& \textbf{$m=1$ prosumers} & \textbf{$m=2$ aggregators}\\
\midrule
Vertices with contraction & 297 / 576 \ (51.6\,\%) & 272 / 576 \ (47.2\,\%)\\
Maximum projection loss & 38.3 kW & 16.1 kW\\
Area loss --- median & 22.2\,\% & 7.5\,\%\\
Area loss --- maximum & \textbf{87.3\,\%} & 23.1\,\%\\
Observed $\max|\zeta|$ \ \nw{(bound \eqref{eq:zbox}, reached)} & 1.000 & 1.000\\
\bottomrule
\end{tabular}
\end{center}

\begin{keybox}
\textbf{Two observations.} \emph{(i)} The loss of \emph{area} is much larger than that of projection
per direction, whose median is practically nil: where the boundary has a flat face
perpendicular to the sweep direction, the optimum slides to a fair point at no cost in that
direction. Both metrics must be reported. \emph{(ii)} Scaling a two-dimensional region by $\gamma$
scales its area by $\gamma^2$: an area loss of 87\,\% is equivalent to a linear shrinkage to
$\sim$36\,\%, not to 13\,\%.
\end{keybox}

The fleet is homogeneous --- $\overline{P}_b = 3.6$ kW in the 1,216 batteries --- so that on this
data the proportional criterion coincides numerically with the egalitarian one. That does not make
it innocuous: the network is radial, and two prosumers with the same battery at different electrical
positions cannot inject the same amount without producing different voltage rises. The
heterogeneity that makes C1 bind is \textbf{locational}, not equipment.


\subsection{Results on TR9 in production}

The same metrics on the production case --- TR9, 90 prosumers, 48 periods $\times$ 12 directions ---
measured against the $m=0$ reference case of the same run:

\begin{center}\small
\begin{tabular}{l r r}
\toprule
& $m=1$ \textbf{prosumers} & $m=2$ \textbf{aggregators}\\
\midrule
Vertices with contraction & 554 / 576 \ (96.2\,\%) & 476 / 576 \ (82.6\,\%)\\
Maximum projection loss & 315.9 kW & 23.6 kW\\
Area loss --- median & 35.8\,\% & 2.3\,\%\\
Area loss --- maximum & 90.1\,\% & 6.8\,\%\\
Observed $\max|\zeta|$ & 1.000 & 1.000\\
Non-optimal vertices & 0 / 576 & 0 / 576\\
\bottomrule
\end{tabular}
\end{center}

The structure repeats at production scale and sharpens: level~1 contracts almost every vertex and
costs a median area loss of 35.8\,\%, whereas level~2 --- which only ties the per-aggregator sums ---
stays at 2.3\,\%. The bound $|\zeta|\le1$ is reached and never exceeded in either, as on the micro
network.

\subsection{Results with the criterion on the exchange ($m=3$, $m=4$)}

On the micro bed, against the same reference case:

\begin{center}\small
\begin{tabular}{l r r}
\toprule
& $m=3$ \textbf{prosumers} & $m=4$ \textbf{aggregators}\\
\midrule
Vertices with contraction & 564 / 564 \ (100\,\%) & 560 / 576 \ (97.2\,\%)\\
Maximum projection loss & 39.7 kW & 18.2 kW\\
Area loss --- median & 38.7\,\% & 15.5\,\%\\
Area loss --- maximum & 95.5\,\% & 35.0\,\%\\
Observed $\zeta$ --- range & $[-1.014,\ 0.865]$ & $[-1.038,\ 0.938]$\\
\textbf{Infeasible} vertices & \textbf{12 / 576} & 0 / 576\\
\bottomrule
\end{tabular}
\end{center}

\begin{keybox}
\textbf{The two warnings of \S\ref{sec:exchange}, measured.} The 12 infeasible vertices of $m=3$ are
\emph{the twelve directions of a single block}, the 22:00 one --- not scattered failures, but a whole
block the criterion leaves without a region. And the bound stops being symmetric: $\zeta\le1$ holds
with room to spare in both, but $\zeta$ drops to $-1.014$ and $-1.038$, below $-1$. This is exactly
what the argument anticipates --- export is bounded by $s_b$, import is not --- and it is why the
realised range is worth reporting rather than assuming.
\end{keybox}


\section{Next steps}

\begin{enumerate}[leftmargin=*,itemsep=3pt]

\item \textbf{Whole-feeder run, without fairness.} This is the validation still missing: a complete
day of 48 periods over the entire network, with $m=0$, to replace the $\sim$2.9-day projections of
\S1.7 with measurement and to confirm optimality and results at that scale. No 48-period run on the
whole feeder has been completed so far; what has been measured are representative periods. It is also
worth carrying the incremental flush \nw{(F)} over to the production sweep beforehand, since its
results file is currently closed only at the end and an interruption of a multi-day run would leave
nothing.

\item \prop{\textbf{Implement $m=5$ and $m=6$.}} \prop{The formulation of \S2.9 is defined but not
built. With the three families available --- proportional to the nameplate (\mbox{$m=1,2$}), on the
exchange (\mbox{$m=3,4$}) and on the available range (\mbox{$m=5,6$}) --- the comparison between
definitions of fairness can be shown measured rather than argued, which is what allows it to be
decided on evidence rather than preference.}

\end{enumerate}

\end{document}
